[package]
name = "usb-init-exerciser"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Standalone firmware: clocks::init → freeze-free USB2 → UsbBus::new → build a CDC device → poll. Proves the STM32H7_OTG Renode model lets the real HAL USB init complete without hanging."

[dependencies]
cortex-m.workspace = true
cortex-m-rt.workspace = true
panic-halt.workspace = true
daisy-bsp.workspace = true
usb-device.workspace = true
usbd-serial.workspace = true
