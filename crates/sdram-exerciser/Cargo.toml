[package]
name = "sdram-exerciser"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Standalone firmware that runs the real daisy_bsp::sdram::init() + a data sweep so Renode's STM32H7_FMC_SDRAM gating model verifies the actual FMC bring-up sequence (not just hand-written command words)."

[dependencies]
cortex-m.workspace = true
cortex-m-rt.workspace = true
panic-halt.workspace = true
daisy-bsp.workspace = true
