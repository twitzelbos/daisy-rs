[package]
name = "fault-exerciser"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Standalone firmware that exercises the Cortex-M7 fault/interrupt vectors so Renode's exception model can be verified against RM0433."

[dependencies]
cortex-m.workspace = true
cortex-m-rt.workspace = true
stm32h7xx-hal.workspace = true
panic-halt.workspace = true
