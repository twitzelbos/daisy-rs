[package]
name = "gap-exerciser"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Standalone firmware that deliberately accesses an unmapped RAM gap (0x2100_0000) so Renode's GapGuard can be verified to fault like silicon instead of silently swallowing it."

[dependencies]
cortex-m.workspace = true
cortex-m-rt.workspace = true
panic-halt.workspace = true
